// Workspace candidate E — Astryx token-override skin of the Cultivate archetype.

import { createFileRoute } from '@tanstack/react-router';
import { AstryxWorkspace } from '@/workspace-candidates/astryx/AstryxWorkspace';

export const Route = createFileRoute('/workspace-e')({
  component: AstryxWorkspace,
});
