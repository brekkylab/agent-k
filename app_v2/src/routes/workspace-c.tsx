// Workspace candidate C — Cultivation Canvas.

import { createFileRoute } from '@tanstack/react-router';
import { CultivationCanvasWorkspace } from '@/workspace-candidates/cultivation-canvas/CultivationCanvasWorkspace';

export const Route = createFileRoute('/workspace-c')({
  component: CultivationCanvasWorkspace,
});
